server:
  port: 8282
  servlet:
    context-path: /HIS
spring:
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  mvc:
    view:
      prefix: /views/
      suffix: .jsp   
  jpa:
   properties:
      hibernate:
       show_sql:true
       format_sql:true
   hibernate:
      ddl-auto: update    
  mail:
    host: smtp.gmail.com
    password: myprojgit
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          connectiontimeout: 5000
          starttls:
            enable: true
          timeout: 5000
          writetimeout: 5000
    username: amyprojectgithub@gmail.com
logging:  level:
    root: INFO    com:
    ashokit:
       service: DEBUG
    
           
           
    
     
    
    
         
      